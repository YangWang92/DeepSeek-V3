from setuptools import setup, find_packages

setup(
    name="inference",
    version="0.1.0",
    packages=find_packages(),
    author="yangwang",
    description="DeepSeek V3 Inference Package",
    python_requires=">=3.8",
)
